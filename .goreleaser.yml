version: 2

before:
  hooks:
    - go mod download

builds:
  - binary: sqlc
    main: ./cmd/sqlc/main.go
    env:
      - CGO_ENABLED=0
    flags:
      - -tags=safe
    ldflags:
      - -s -w
      - -X github.com/sqlc-dev/sqlc/internal/cmd.version={{.Version}}
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
      - arm64

archives:
  - formats: [ tar.gz ]
    format_overrides:
      - goos: windows
        formats: [ zip ]

changelog:
  use: github-native
